# Telegram-бот для генерации идей вайб-кодинга

Бот помогает экспертам и предпринимателям найти идеи для создания веб-приложений и лендингов через AI-инструменты.

## Структура проекта

```
vibecoding-bot/
├── bot.py              # Основной файл бота
├── llm.py              # Работа с OpenRouter API
├── prompts.py          # Системный промпт для LLM
├── logger.py           # Логирование в файл
├── vibes_image.jpg     # Картинка для ВАЙБС
├── .env.example        # Пример переменных окружения
├── requirements.txt    # Зависимости
├── Procfile            # Конфигурация для Railway
├── runtime.txt         # Версия Python для Railway
└── logs/
    └── conversations.log   # Логи диалогов
```

## Установка

### 1. Клонируйте проект и перейдите в директорию

```bash
cd vibecoding-bot
```

### 2. Создайте виртуальное окружение (рекомендуется)

```bash
python3 -m venv venv
source venv/bin/activate  # для Linux/macOS
# или
venv\Scripts\activate  # для Windows
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Настройте переменные окружения

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

Откройте `.env` и заполните токены:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_botfather
OPENROUTER_API_KEY=ваш_ключ_openrouter
```

## Получение токенов

### Как получить TELEGRAM_BOT_TOKEN

1. Откройте Telegram и найдите [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Введите имя бота (например: "Vibecoding Ideas Bot")
4. Введите username бота (должен заканчиваться на `bot`, например: `vibecoding_ideas_bot`)
5. BotFather пришлёт вам токен вида: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`
6. Скопируйте этот токен в `.env` файл

### Как получить OPENROUTER_API_KEY

1. Перейдите на [OpenRouter.ai](https://openrouter.ai/)
2. Нажмите "Sign In" в правом верхнем углу
3. Войдите через Google, GitHub или Email
4. После входа перейдите в раздел "Keys" или "API Keys"
5. Нажмите "Create Key" или "Generate New Key"
6. Скопируйте созданный ключ (начинается с `sk-or-v1-...`)
7. Вставьте его в `.env` файл

**Важно:** На OpenRouter нужно пополнить баланс минимум на $1-5 для использования API. Модель `tngtech/deepseek-r1t2-chimera:free` бесплатная, но требует наличие аккаунта с активным балансом.

## Запуск бота

```bash
python bot.py
```

Вы увидите сообщение:
```
🤖 Бот запущен и готов к работе!
Нажмите Ctrl+C для остановки
```

## Использование

1. Найдите вашего бота в Telegram по username
2. Нажмите `/start`
3. Опишите свою нишу или сферу деятельности
4. Получите 3 идеи проектов для вайб-кодинга
5. Попросите раскрыть любую идею подробнее

## Логирование

Все диалоги сохраняются в файл `logs/conversations.log` в формате:

```
[2025-12-19 14:32:01] user_id=123456 username=@ivan_petrov message="Я психолог" response="Отлично, психология..."
```

## Технологии

- **Python 3.11+**
- **aiogram 3.x** — современный асинхронный фреймворк для Telegram Bot API
- **httpx** — асинхронный HTTP-клиент для запросов к OpenRouter
- **python-dotenv** — загрузка переменных окружения из `.env` файла

## Остановка бота

Нажмите `Ctrl+C` в терминале для корректной остановки бота.

## Деплой на Railway

Бот готов к деплою на [Railway](https://railway.app/) из коробки!

### Шаги деплоя:

1. **Зарегистрируйтесь на Railway:**
   - Перейдите на [Railway.app](https://railway.app/)
   - Войдите через GitHub

2. **Создайте новый проект:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите репозиторий с ботом

3. **Настройте переменные окружения:**
   - В панели проекта перейдите в "Variables"
   - Добавьте все переменные из `.env.example`:
     - `TELEGRAM_BOT_TOKEN` — токен вашего бота
     - `OPENROUTER_API_KEY` — ключ OpenRouter API
     - `LIVE_STREAM_URL` — ссылка на прямой эфир YouTube

4. **Деплой произойдёт автоматически!**
   - Railway автоматически обнаружит `Procfile`
   - Установит зависимости из `requirements.txt`
   - Запустит бота

5. **Проверьте логи:**
   - В панели Railway откройте вкладку "Deployments"
   - Убедитесь, что видите сообщение: "🤖 Бот запущен и готов к работе!"

### Важные файлы для деплоя:

- `Procfile` — команда запуска бота
- `requirements.txt` — зависимости Python
- `runtime.txt` — версия Python (3.9)
- `.gitignore` — исключает `.env` и логи из репозитория

